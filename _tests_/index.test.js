const index = require('../utils/index.js');

test('Is a sum equal to 4', () => {
  expect(weGetData(2, 2)).toBe(4);
});


// test('Drag data is a number bigger than 3', () => {
//   expect(dragData('site')).toBeGreaterThanOrEqual(3.5);
// });
